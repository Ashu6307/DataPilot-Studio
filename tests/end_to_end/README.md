# End-to-end tests

Browser-driven coverage is configured in `apps/web/e2e`. It runs against local Vite and FastAPI processes and covers the guided vertical-slice journey. API-only integration coverage remains under `tests/integration`.

