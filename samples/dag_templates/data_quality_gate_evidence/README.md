# Data Quality Gate and Evidence Export

Anonymised visual workflow template.

Expected execution path: `source → quality rules → approval gate → Excel evidence`.

UI walkthrough: create a local project, open Visual Workflow Studio, load this template, bind the sample source parameters, validate, inspect the plan, publish, and run.
